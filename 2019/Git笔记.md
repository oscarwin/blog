## Git 最小配置

```
// 配置用户名
git config --global user.name "oscarwin"
// 配置用户邮箱
git config --global user.email "oscarwin@xx.com"

PS:
--local 针对当前某个仓库
--global 针对当前用户所有仓库
--system 针对该系统上所有用户

// 查看配置
git config --list --local
git config --list --global
git config --list --system
```


